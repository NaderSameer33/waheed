import 'package:flutter/material.dart';

class ReciveFromShop extends StatelessWidget {
  const ReciveFromShop({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(child: Text('recvie shop'),) ;
  }
}