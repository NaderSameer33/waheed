import 'package:flutter/material.dart';

class ReciveFromHome extends StatelessWidget {
  const ReciveFromHome({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('recvie home'),
    );
  }
}
